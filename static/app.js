const $=s=>document.querySelector(s); const chatEl=$("#chat"), input=$("#input"), sendBtn=$("#sendBtn"), modelSelect=$("#modelSelect");
const KEY="ai_canvas_chats_v3"; let chats=loadChats(), currentId=null, messages=[], attachments=[], busy=false;
function loadChats(){try{return JSON.parse(localStorage.getItem(KEY)||"[]")}catch{return[]}}
function saveChats(){localStorage.setItem(KEY,JSON.stringify(chats));$("#saveState").textContent="Saved locally"}
function uid(){return crypto.randomUUID?crypto.randomUUID():Date.now()+"-"+Math.random().toString(16).slice(2)}
function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function md(s){let x=escapeHtml(s);x=x.replace(/```([\s\S]*?)```/g,(_,c)=>`<pre><code>${c.trim()}</code></pre>`).replace(/`([^`]+)`/g,'<code>$1</code>').replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>').replace(/\*([^*]+)\*/g,'<em>$1</em>').replace(/\n/g,'<br>');return x}
function toast(m){const t=$("#toast");t.textContent=m;t.classList.add("show");setTimeout(()=>t.classList.remove("show"),2200)}
function resize(){input.style.height="auto";input.style.height=Math.min(input.scrollHeight,180)+"px"} input.addEventListener("input",resize);
function makeChat(){const id=uid();return{id,title:"New chat",created:Date.now(),updated:Date.now(),messages:[]}}
function current(){return chats.find(c=>c.id===currentId)}
function renderHistory(){const q=$("#chatSearch").value.toLowerCase();const box=$("#history");box.innerHTML="";chats.filter(c=>c.title.toLowerCase().includes(q)).sort((a,b)=>b.updated-a.updated).forEach(c=>{const row=document.createElement("div");row.className="history-row"+(c.id===currentId?" active":"");row.innerHTML=`<button class="chat-item"><span class="chat-icon">▱</span><span class="chat-title">${escapeHtml(c.title)}</span></button><button class="more-btn" title="Chat options">⋯</button>`;row.querySelector(".chat-item").onclick=()=>openChat(c.id);row.querySelector(".more-btn").onclick=e=>{e.stopPropagation();openMenu(e,c.id)};box.appendChild(row)})}
function openMenu(e,id){const m=$("#contextMenu");m.innerHTML=`<button data-act="rename">✎ Rename</button><button data-act="download">⇩ Download workspace</button><button data-act="delete" class="danger">⌫ Delete chat</button>`;m.style.left=Math.min(e.clientX,innerWidth-220)+"px";m.style.top=Math.min(e.clientY,innerHeight-150)+"px";m.classList.add("show");m.querySelector('[data-act="rename"]').onclick=()=>{const c=chats.find(x=>x.id===id);const n=prompt("Rename chat",c.title);if(n?.trim()){c.title=n.trim();c.updated=Date.now();saveChats();renderHistory()}m.classList.remove("show")};m.querySelector('[data-act="download"]').onclick=()=>{m.classList.remove("show");downloadChat(id)};m.querySelector('[data-act="delete"]').onclick=()=>{if(confirm("Delete this chat from local history?")){chats=chats.filter(x=>x.id!==id);if(currentId===id)newChat();saveChats();renderHistory()}m.classList.remove("show")}}
document.addEventListener("click",e=>{if(!e.target.closest(".context-menu,.more-btn"))$("#contextMenu").classList.remove("show")});
function renderMessages(){chatEl.innerHTML="";if(!messages.length){chatEl.innerHTML=`<div class="hero" id="hero"><div class="hero-orb">✦</div><h1>What will you build?</h1><p>Chat, upload files, create outputs, and export the entire workspace.</p><div class="suggestions"><button data-prompt="Analyze my project architecture and suggest improvements.">Analyze architecture</button><button data-prompt="Review my code and find bugs.">Review code</button><button data-prompt="Create a practical step-by-step plan.">Make a plan</button><button data-prompt="Create a downloadable Markdown report from this conversation.">Create report</button></div></div>`;bindSuggestions();return}messages.forEach(m=>addMessage(m.role,m.content,false));chatEl.scrollTop=chatEl.scrollHeight}
function addMessage(role,text,scroll=true){$("#hero")?.remove();const row=document.createElement("div");row.className=`msg-row ${role}`;const av=document.createElement("div");av.className="avatar";av.textContent=role==="user"?"You":"✦";const bubble=document.createElement("div");bubble.className="bubble";bubble.innerHTML=md(text);row.append(av,bubble);chatEl.appendChild(row);if(scroll)chatEl.scrollTop=chatEl.scrollHeight;return{row,bubble}}
function addTool(name,result){const row=document.createElement("div");row.className="msg-row tool-row";row.innerHTML=`<div class="avatar">⚙</div><div class="bubble"><div class="tool-card"><b>⚙ ${escapeHtml(name)}</b><span>${escapeHtml(result).slice(0,900)}</span></div></div>`;chatEl.appendChild(row);chatEl.scrollTop=chatEl.scrollHeight}
function newChat(){const c=makeChat();chats.unshift(c);currentId=c.id;messages=[];attachments=[];renderMessages();renderAttachments();renderHistory();saveChats();$("#sidebar").classList.remove("open");input.focus()}
function openChat(id){const c=chats.find(x=>x.id===id);if(!c)return;currentId=id;messages=c.messages||[];attachments=[];renderMessages();renderAttachments();renderHistory();$("#sidebar").classList.remove("open")}
function updateCurrent(){const c=current();if(!c)return;c.messages=messages;c.updated=Date.now();if(messages.length&&c.title==="New chat"){const first=messages.find(m=>m.role==="user")?.content||"New chat";c.title=first.replace(/\s+/g," ").slice(0,52)}saveChats();renderHistory()}
async function loadModels(){try{const r=await fetch("/api/models");const d=await r.json();modelSelect.innerHTML="";d.models.forEach(m=>{const o=document.createElement("option");o.value=m;o.textContent=m;modelSelect.appendChild(o)});const p=d.models.find(m=>m.toLowerCase().includes("luna"))||d.models[0];if(p)modelSelect.value=p}catch{toast("Could not load models")}}
async function health(){try{const d=await (await fetch("/api/health")).json();$("#healthDot").style.background=d.api_configured?"#5eead4":"#ffb86b";$("#healthText").textContent=d.api_configured?"Gateway connected":"API key missing"}catch{$("#healthText").textContent="Server unavailable"}}
function setBusy(v){busy=v;sendBtn.disabled=v;input.disabled=v;$("#attachBtn").disabled=v;sendBtn.textContent=v?"…":"➤"}
async function uploadFiles(fileList){if(!currentId)return;const files=[...fileList];if(!files.length)return;const fd=new FormData();fd.append("chat_id",currentId);files.forEach(f=>fd.append("files",f));try{const r=await fetch("/api/upload",{method:"POST",body:fd});if(!r.ok)throw new Error(await r.text());const d=await r.json();attachments.push(...d.files);renderAttachments();toast(`${d.files.length} file(s) uploaded`)}catch(e){toast("Upload failed: "+e.message)}}
function renderAttachments(){const box=$("#attachments");box.innerHTML="";attachments.forEach((f,i)=>{const el=document.createElement("div");el.className="attachment";el.innerHTML=`<span>📎</span><span>${escapeHtml(f.name)}</span><button title="Remove">×</button>`;el.querySelector("button").onclick=()=>{attachments.splice(i,1);renderAttachments()};box.appendChild(el)})}
async function send(text){if(busy||!text.trim())return;if(!currentId)newChat();text=text.trim();const extra=attachments.length?`\n\n[Attached files: ${attachments.map(x=>x.stored).join(", ")}]`:"";addMessage("user",text);messages.push({role:"user",content:text+extra});attachments=[];renderAttachments();const ai=addMessage("assistant","");ai.bubble.innerHTML='<span class="thinking">Thinking…</span>';setBusy(true);let answer="";try{const res=await fetch("/api/chat/stream",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({chat_id:currentId,model:modelSelect.value,messages,tools_enabled:$("#toolsToggle").checked})});if(!res.ok)throw new Error(await res.text());const reader=res.body.getReader(),dec=new TextDecoder();let buf="";while(true){const {value,done}=await reader.read();if(done)break;buf+=dec.decode(value,{stream:true});const events=buf.split("\n\n");buf=events.pop()||"";for(const raw of events){const dm=raw.match(/^data:\s*(.*)$/m);if(!dm)continue;let data;try{data=JSON.parse(dm[1])}catch{continue}const type=(raw.match(/^event:\s*(.*)$/m)||[])[1];if(type==="token"){answer+=data.text;ai.bubble.innerHTML=md(answer)+'<span class="cursor"></span>';chatEl.scrollTop=chatEl.scrollHeight}else if(type==="tool_start"){addTool(data.name,"Running…")}else if(type==="tool_result"){addTool(data.name,data.result)}else if(type==="status"&&!answer){ai.bubble.innerHTML=`<span class="thinking">${escapeHtml(data.message)}</span>`}else if(type==="error"){ai.bubble.innerHTML=`<span class="error-text">${escapeHtml(data.message)}</span>`;toast("Request failed")}}}ai.bubble.innerHTML=md(answer||"");if(answer)messages.push({role:"assistant",content:answer});updateCurrent()}catch(e){ai.bubble.innerHTML=`<span class="error-text">${escapeHtml(e.message)}</span>`}finally{setBusy(false);input.focus()}}
async function downloadChat(id=currentId){const c=chats.find(x=>x.id===id);if(!c)return;try{const r=await fetch("/api/download",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({chat_id:id,title:c.title,messages:c.messages||[]})});if(!r.ok)throw new Error(await r.text());const blob=await r.blob(),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=(c.title.replace(/[^\w.-]+/g,"_")||"AI_Canvas_Chat")+".zip";a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast("Workspace download started")}catch(e){toast("Download failed: "+e.message)}}
function bindSuggestions(){document.querySelectorAll("[data-prompt]").forEach(b=>b.onclick=()=>{input.value=b.dataset.prompt;resize();input.focus()})}
$("#composer").onsubmit=e=>{e.preventDefault();const v=input.value;input.value="";resize();send(v)};input.onkeydown=e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();$("#composer").requestSubmit()}};$("#newChat").onclick=newChat;$("#downloadTop").onclick=()=>downloadChat();$("#clearBtn").onclick=()=>{messages=[];updateCurrent();renderMessages();toast("Current chat cleared")};$("#menuBtn").onclick=()=>$("#sidebar").classList.toggle("open");$("#chatSearch").oninput=renderHistory;$("#clearHistory").onclick=()=>{if(confirm("Clear all local chat history?")){chats=[];newChat();saveChats();renderHistory()}};$("#attachBtn").onclick=()=>$("#fileInput").click();$("#fileInput").onchange=e=>uploadFiles(e.target.files);document.addEventListener("dragover",e=>e.preventDefault());document.addEventListener("drop",e=>{e.preventDefault();if(e.dataTransfer.files?.length)uploadFiles(e.dataTransfer.files)});
if(!chats.length)newChat();else openChat(chats.sort((a,b)=>b.updated-a.updated)[0].id);bindSuggestions();loadModels();health();setInterval(health,30000);
